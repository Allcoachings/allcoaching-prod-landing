{# Site-wide minimal header — matches legacy chrome exactly. #}
<header class="fixed top-0 inset-x-0 z-50">
  <div class="max-w-7xl mx-auto px-4 md:px-5 mt-4">
    <nav class="nav-shell flex items-center justify-between px-4 py-2.5">
      <span class="nav-shell-halo" aria-hidden="true"></span>
      <a href="/" aria-label="{{ site.name }} home" class="flex items-center">
        <img src="{{ assets_prefix }}assets/logo/allcoaching-logo.webp?v=20260707" alt="{{ site.name }}" class="nav-logo h-9 md:h-10 w-auto" decoding="async" width="512" height="512" />
      </a>
      <div class="flex items-center gap-2">
        <a href="{{ site.ctas.educator_signup }}" target="_blank" rel="noopener" class="btn btn-ghost hidden md:inline-flex">Login</a>
        <a href="{{ site.ctas.educator_signup }}" target="_blank" rel="noopener" aria-label="Join Now" class="btn btn-primary btn-cta-mobile">
          <span class="btn-label">Join Now</span>
        </a>
      </div>
    </nav>
  </div>
</header>
